**DAILY GIT FLOW**

**BEFORE TASK**
1. git pull -u origin main(once), use git pull
2. # code ...
3. git add .
4. git commit 
5. git push -u origin main(once), use git push

**AFTER TASK**
1. git checkout main
2. git pull -u origin main(once)
3. git merge branch-name
4. git push -u origin main